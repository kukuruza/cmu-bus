This packages 'entities' and 'protocols' from this package ('common') are used both by the app the the server. To avoid duplication, webserver 'imports file system' from the app directory structure, that is, it shares these packages with the CmuBusTracker eclipse project.

Look for CmuBusTracker/src/cmu18641/bustracker/common
